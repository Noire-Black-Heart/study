ADT -- abstract data type -- 抽象数据类型

抽象数据类型是指一个数学模型以及定义在此数学模型上的一组操作

------------------

iterator -- 迭代器

提供一种方法访问一个容器（container）对象中各个元素，而又不需暴露该对象的内部细节。

不访问对象本身即可遍历其中每一个元素（并给予提取）

重点：遍历
-----------------

